// Backed by the live users store (demo contacts seeded + real users synced from
// POST /api/users/sync merged on top). See src/store/usersStore.ts.
export { contactById } from '@/store/usersStore';
